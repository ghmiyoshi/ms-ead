eureka:
  client:
    fetch-registry: false
    register-with-eureka: false

spring:
  application:
    name: eureka-server

server:
  port: 8761

ead:
  service-registry:
    username: guest
    password: guest
